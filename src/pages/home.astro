---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Remember - Master Any Memory Like a Pro with Science-Backed Learning">
  <main class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
    
    <!-- Hero Section -->
    <section class="flex items-center justify-center min-h-screen p-4 lg:p-8">
      <div class="max-w-7xl mx-auto">
        <div class="grid grid-cols-1 lg:grid-cols-5 gap-8 lg:gap-12 items-center">
          
          <!-- Text Content -->
          <div class="lg:col-span-2 text-center lg:text-left order-2 lg:order-1">
            <!-- Main Headline -->
            <h1 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-bold text-gray-800 mb-3 lg:mb-4 animate-fade-in leading-tight">
              Master Any Memory
              <br />
              <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                Like a Pro! 🚀
              </span>
            </h1>

            <!-- Sub-headline -->
            <p class="text-base sm:text-lg md:text-xl text-gray-600 mb-4 lg:mb-6 font-medium animate-fade-in-delay-1">
              Powered by <strong>Spaced Repetition</strong> & <strong>SM-2 Algorithm</strong>
              <br />
              <span class="text-sm sm:text-base md:text-lg text-gray-500">
                The same science used by memory champions ⭐
              </span>
            </p>

            <!-- CTA Button -->
            <div class="animate-fade-in-delay-2 mb-4 lg:mb-6">
              <a
                href="/"
                class="inline-block bg-gradient-to-r from-blue-600 to-purple-600 text-white px-5 sm:px-6 py-2.5 sm:py-3 rounded-full text-base sm:text-lg font-semibold hover:from-blue-700 hover:to-purple-700 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl"
              >
                🚀 Start Learning - FREE!
              </a>
              
              <div class="mt-2 sm:mt-3 text-gray-500 text-xs sm:text-sm">
                No signup required • Works instantly • All devices supported
              </div>
            </div>
          </div>

          <!-- App Preview Image -->
          <div class="lg:col-span-3 order-1 lg:order-2 animate-fade-in-delay-3">
            <div class="relative max-w-md mx-auto lg:max-w-lg xl:max-w-xl 2xl:max-w-2xl">
              <!-- Real App Screenshot -->
              <div class="relative">
                <img 
                  src="/screenshot.png" 
                  alt="Remember app interface showing flashcard learning with Chinese characters"
                  class="w-full h-auto rounded-2xl"
                  loading="lazy"
                />
              </div>
              
              <!-- Floating Elements -->
              <div class="absolute -top-4 -right-4 bg-green-100 text-green-600 px-3 py-1 rounded-full text-sm font-medium animate-bounce">
                Nailed it! 🎉
              </div>
              <div class="absolute -bottom-4 -left-4 bg-blue-100 text-blue-600 px-3 py-1 rounded-full text-sm font-medium animate-pulse">
                SM-2 Algorithm
              </div>
            </div>
          </div>
          
        </div>
      </div>
    </section>

    <!-- Content Section -->
    <section class="px-4 lg:px-8 pb-16">
      <div class="max-w-4xl mx-auto text-center">

      <!-- Stats or Features Preview -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 sm:gap-6 max-w-3xl mx-auto animate-fade-in-delay-4 mb-12 sm:mb-16">
        <div class="text-center p-4 bg-white/50 rounded-xl backdrop-blur-sm">
          <div class="text-2xl sm:text-3xl mb-2">🧠</div>
          <div class="font-semibold text-gray-800 text-sm sm:text-base">Remember Longer</div>
          <div class="text-xs sm:text-sm text-gray-600">Science-backed SM-2</div>
        </div>
        
        <div class="text-center p-4 bg-white/50 rounded-xl backdrop-blur-sm">
          <div class="text-2xl sm:text-3xl mb-2">⚡</div>
          <div class="font-semibold text-gray-800 text-sm sm:text-base">Save Time</div>
          <div class="text-xs sm:text-sm text-gray-600">Focus on what matters</div>
        </div>
        
        <div class="text-center p-4 bg-white/50 rounded-xl backdrop-blur-sm">
          <div class="text-2xl sm:text-3xl mb-2">🎯</div>
          <div class="font-semibold text-gray-800 text-sm sm:text-base">Kid-Friendly</div>
          <div class="text-xs sm:text-sm text-gray-600">Simple & intuitive</div>
        </div>
      </div>

      <!-- Created by story -->
      <div class="text-center text-gray-500 text-xs sm:text-sm animate-fade-in-delay-5">
        💝 Created with love by a dad who wanted effective learning for his child
        <br />
        <a 
          href="https://medium.com/p/f2875a0f8277" 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-blue-600 hover:text-blue-700 underline mt-2 inline-block"
        >
          Read the full story →
        </a>
      </div>
      
    </div>
    </section>
  </main>
</Layout>

<style>
  /* Custom animations using CSS */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.8s ease-out;
  }

  .animate-fade-in-delay-1 {
    animation: fadeIn 0.8s ease-out 0.2s both;
  }

  .animate-fade-in-delay-2 {
    animation: fadeIn 0.8s ease-out 0.4s both;
  }

  .animate-fade-in-delay-3 {
    animation: fadeIn 0.8s ease-out 0.6s both;
  }

  .animate-fade-in-delay-4 {
    animation: fadeIn 0.8s ease-out 0.8s both;
  }

  .animate-fade-in-delay-5 {
    animation: fadeIn 0.8s ease-out 1.0s both;
  }

  .animate-fade-in-delay-6 {
    animation: fadeIn 0.8s ease-out 1.2s both;
  }
  
  /* Additional responsive utilities */
  @media (max-width: 640px) {
    .text-responsive-xl {
      font-size: 1.5rem;
      line-height: 2rem;
    }
  }
  
  @media (min-width: 641px) {
    .text-responsive-xl {
      font-size: 2rem;
      line-height: 2.5rem;
    }
  }
  
  /* Fix for mobile layout */
  html, body {
    overflow: auto;
  }
  
  /* Mobile optimizations */
  @media (max-width: 1024px) {
    main {
      overflow-x: hidden;
    }
  }
</style>
